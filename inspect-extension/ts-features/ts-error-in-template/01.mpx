<template>
  {{ knownProp1 }}
  <!-- 这里预期会触发报错 TS Error: Property 'error_unknownProp' does not exist on type [...]. ts-plugin(2339) -->
  {{ error_unknownProp }}
  {{ knownProp2 }}
  <!-- @mpx-expect-error 此注释会忽略预期报错：Property 'suppressed_error_unknownProp' does not exist on type [...]. ts-plugin(2339) -->
  {{ suppressed_error_unknownProp }}
  {{ knownProp3 }}
  <!-- @mpx-expect-error 此注释多余，所以会触发注释报错 TS Error: Unused '@ts-expect-error' directive.ts-plugin(2578) -->
  {{ knownProp4_will_trigger_unused_expect_error }}
</template>

<script setup lang="ts">
defineProps<{
    knownProp1: string
    knownProp2: string
    knownProp3: string
    knownProp4_will_trigger_unused_expect_error: string
}>()
</script>
